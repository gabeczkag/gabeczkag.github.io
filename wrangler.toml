name = "gabeczkaweb-authorization"
main = "src/worker.js"
type = "module"
compatibility_date = "2024-09-23"

# Sekrety (Settings -> Variables and Secrets w Cloudflare):
#   GITHUB_CLIENT_SECRET   (z OAuth App na GitHubie)
#   SESSION_SECRET         (dowolny losny ciąg, np. `openssl rand -hex 32`)
#
# Reszta konfiguracji (client_id, worker_url, site_origin) jest zahardkodowana w src/worker.js
